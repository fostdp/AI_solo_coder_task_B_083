[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -v --strict-markers
markers =
    asyncio: 标记异步测试
    integration: 标记集成测试
    unit: 标记单元测试
